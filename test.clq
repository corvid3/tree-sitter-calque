requires "another_file.clq"

let m = \x y ->
	let s = x, y + in
	2, 3 +, s * display
in

type Player -> Entity Damagable in
type Foobar -> Foo Bar Baz in


let add2 =
	\val -> $ 2 +
in

method someMethod (x Integer) ->
	x, 2 + display
in

method someMethod (x Float) ->
	x, 3.0 + display
in

method someMethod (x Integer) (y Integer) ->
	x, y + display
in

method someMethod (... Any) ->

let add3 =
	\val -> $ 3 +
in

let m = \y -> 2 transform. in
m transform
"hello, world!" display;
2, 3 add display.

let select = \s ->
	if s, 1 eq
	then add2
	else add3
in

let y = \a ->
	let m =
		\val -> let f = 1 select in m f
	in
	some-list, m map
in


type Player -> Entity in
type Crate -> Entity in

method DoThing (ent Entity) ->
	ent 10 ?Damage
in

method Damage (player Player) (val Number) ->
	player:health val - set! player:health
in

